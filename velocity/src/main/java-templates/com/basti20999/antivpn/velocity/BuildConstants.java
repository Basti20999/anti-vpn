package com.basti20999.antivpn.velocity;

/** Filled in from the Maven version by templating-maven-plugin. */
final class BuildConstants {

    static final String VERSION = "${project.version}";

    private BuildConstants() {
    }
}
